import React from 'react';
import ReactDOM from 'react-dom';  // No changes needed here
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
